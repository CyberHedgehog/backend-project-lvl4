extends /layouts/main.pug

block title
  title= t('views.pages.startPage.greeting')

block main
  main(class="mx-auto text-center col-md-6")
      h1= t('views.pages.startPage.greeting')
      p= t('views.pages.startPage.text')
      //- - const [msg] = flash('info');
      //- if msg
        +alert('success')
        a(href="/tasks" class="btn btn-light mr-2") Go to tasks!
      //- else  
        a(href="/login" class="btn btn-light") Get started
